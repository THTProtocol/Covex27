================================================================================
  URGENT: STRICT PAID-ONLY ENFORCEMENT (what the user is screaming for)
================================================================================

The free deploy page is now HARD BLOCKED for anyone with a paid tier.

When a paid user tries to visit /deploy they see a big lock screen:
- "You Paid for Premium"
- "The free deployment page is disabled for paid tier users."
- Big buttons to "Go to Your Covenants + Terminal" and "Open Full Premium Builder"
- Footer: "Free deploy is intentionally blocked. You paid for the real tools."

This is the behavior they want.

To make it live on hightable.pro RIGHT NOW (copy-paste after you are ssh'd as root):

cd /root/Covex27 && git fetch origin && git reset --hard origin/master && cd frontend && npm install && npm run build && systemctl reload nginx || true && echo "=== STRICT PAID ENFORCEMENT LIVE ==="

Then hard refresh the browser (Ctrl+Shift+R) on https://hightable.pro/pricing and test clicking a paid tier.

After payment confirmation they should only see the clean covenants list + Go to Terminal, and /deploy should be blocked with the premium message.

