{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://covex.pro/schemas/covenant-config-v1.json",
  "title": "Covex Covenant Configuration v1",
  "description": "Shared configuration format between Covex Terminal and Covenant Studio. Version 1.0. Stable for Phase 11 integration.",
  "type": "object",
  "required": ["version", "covenant", "resolution", "ui"],
  "properties": {
    "version": {
      "type": "string",
      "const": "1.0",
      "description": "Schema version. Must be '1.0' for this spec."
    },
    "covenant": {
      "type": "object",
      "required": ["id", "name", "description", "creatorAddress"],
      "properties": {
        "id": { "type": "string", "format": "uuid", "description": "Unique covenant identifier" },
        "name": { "type": "string", "minLength": 3, "maxLength": 80 },
        "description": { "type": "string", "maxLength": 500 },
        "creatorAddress": { "type": "string", "description": "Kaspa address of the creator" },
        "createdAt": { "type": "string", "format": "date-time" },
        "reusable": { "type": "boolean", "default": false },
        "allowTopups": { "type": "boolean", "default": false }
      }
    },
    "resolution": {
      "type": "object",
      "required": ["mode", "circuit", "oracle", "payoutModel"],
      "properties": {
        "mode": {
          "type": "string",
          "enum": ["zk", "oracle", "custom_oracle"],
          "description": "Resolution mode"
        },
        "circuit": {
          "type": "object",
          "required": ["type"],
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "merkle_membership",
                "range_proof",
                "hash_preimage",
                "timelock_absolute",
                "relative_timelock",
                "turn_timer",
                "basic_utxo_ownership",
                "script_constraint",
                "pot_split_math",
                "nullifier_set",
                "privacy_mixer_v1",
                "chess_v1",
                "tictactoe_v1",
                "connect4_v1",
                "auction_clearing",
                "onchain_sig_verify",
                "age_verification",
                "verifiable_compute",
                "custom"
              ]
            },
            "verifierKey": { "type": "string", "description": "On-chain or oracle verifier identifier" },
            "provingMode": {
              "type": "string",
              "enum": ["full-zk", "hybrid", "oracle-attested"],
              "default": "hybrid",
              "description": "full-zk = strict Groth16; hybrid = Groth16 when proof body present else oracle attestation; oracle-attested = trust requested_outcome"
            },
            "publicInputs": {
              "type": "object",
              "additionalProperties": true,
              "description": "Public inputs schema for the circuit"
            },
            "timelock": {
              "type": "object",
              "description": "User-configurable clock limits (maps to circuit public signals)",
              "properties": {
                "mode": {
                  "type": "string",
                  "enum": ["absolute", "relative", "turn_timer"],
                  "description": "absolute = timelock_absolute; relative = relative_timelock; turn_timer = per-turn DAA window"
                },
                "lock_threshold_daa": { "type": "integer", "minimum": 0, "description": "timelock_absolute: current_daa must be >= this" },
                "reference_daa": { "type": "integer", "minimum": 0, "description": "relative_timelock: anchor DAA" },
                "lock_duration_daa": { "type": "integer", "minimum": 0, "description": "relative_timelock: minimum blocks since reference" },
                "max_delta_daa": { "type": "integer", "minimum": 1, "description": "turn_timer: max blocks since last_move_daa" }
              }
            }
          }
        },
        "oracle": {
          "type": "object",
          "properties": {
            "provider": {
              "type": "string",
              "enum": ["covex", "custom", "multi"],
              "default": "covex"
            },
            "customPublicKey": { "type": "string" },
            "threshold": {
              "type": "integer",
              "minimum": 1,
              "description": "For multi-oracle: minimum signatures required"
            },
            "providers": {
              "type": "array",
              "items": { "type": "string" },
              "description": "List of oracle public keys when using multi-oracle"
            }
          }
        },
        "payoutModel": {
          "type": "object",
          "required": ["type", "feeBasisPoints"],
          "properties": {
            "type": {
              "type": "string",
              "enum": ["winner_takes_all", "proportional", "custom"]
            },
            "feeBasisPoints": { "type": "integer", "minimum": 0, "maximum": 1000 },
            "platformShare": { "type": "integer", "minimum": 0, "maximum": 10000 },
            "creatorShare": { "type": "integer", "minimum": 0, "maximum": 10000 }
          }
        },
        "constraints": {
          "type": "object",
          "description": "On-chain enforceable constraints (silverc level)",
          "properties": {
            "minStake": { "type": "integer" },
            "maxStake": { "type": "integer" },
            "deadline": { "type": "string", "format": "date-time" },
            "timelock": {
              "type": "object",
              "description": "Alias of resolution.circuit.timelock for covenant scripts",
              "properties": {
                "mode": { "type": "string", "enum": ["absolute", "relative", "turn_timer"] },
                "lock_threshold_daa": { "type": "integer", "minimum": 0 },
                "reference_daa": { "type": "integer", "minimum": 0 },
                "lock_duration_daa": { "type": "integer", "minimum": 0 },
                "max_delta_daa": { "type": "integer", "minimum": 1 }
              }
            }
          }
        }
      }
    },
    "ui": {
      "type": "object",
      "required": ["templateId", "theme"],
      "properties": {
        "templateId": { "type": "string", "description": "ID of the Covenant Studio template" },
        "theme": {
          "type": "object",
          "properties": {
            "primaryColor": { "type": "string", "pattern": "^#[0-9A-Fa-f]{6}$" },
            "backgroundColor": { "type": "string" },
            "fontFamily": { "type": "string" }
          }
        },
        "customizations": {
          "type": "object",
          "additionalProperties": true,
          "description": "Template-specific overrides"
        },
        "bundleUrl": {
          "type": "string",
          "format": "uri",
          "description": "URL to the custom HTML/JS/CSS bundle (if uploaded)"
        }
      }
    },
    "metadata": {
      "type": "object",
      "additionalProperties": true,
      "description": "Free-form metadata for future extensibility"
    }
  },
  "additionalProperties": false
}